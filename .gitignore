# Python
__pycache__/
*.pyc
.venv/

# Django
db.sqlite3

# Build artifacts
build/
dist/
*.spec

# Editors / OS
.vscode/
.DS_Store

# Flutter (frontend/ has its own .gitignore for deeper paths)
frontend/build/
frontend/.dart_tool/

# Django collectstatic output (rebuilt in the Docker image)
server/staticfiles/

# filled-in deployment secrets (only the .example template is tracked)
deploy/*.env
!deploy/*.env.example
